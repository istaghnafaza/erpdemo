# Fallback jika Railway tidak pakai Dockerfile — pakai Node 22.14+
[phases.setup]
nixpkgsArchive = "11cb3517b3af6af300dd6c055aeda73c9bf52c48"

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .output/server/index.mjs"
